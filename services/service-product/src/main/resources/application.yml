server:
  port: 9000

spring:
  application:
    name: service-product

  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      config:
        import-check:
          enabled: false
      discovery:
        namespace: dev

  data:
    redis:
      host: 127.0.0.1
      port: 6379